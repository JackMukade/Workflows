name: First Workflow
on: [push]

jobs:
  run-commands:
    runs-on: ubuntu-latest
    steps:
      -name: echo a string
       run: echo "HELLO WORLD"
      -name: Multiline Command
       run : | 
        node -v
        npm  -v