name: First Workflow
on: [push]

jobs:
  run-commands:
    runs-on: ubuntu-latest
    steps:
      - name: HOLA MUNDO 
        run: echo "HELLO WORLD"
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Azure CLI script
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            az account show